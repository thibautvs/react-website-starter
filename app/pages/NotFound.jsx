import React from 'react';
//import './Home.scss';

export default React.createClass({
  render() {
    return (
      <section id='not-found'>
        <header>Not found</header>
      </section>
    );
  }
});
