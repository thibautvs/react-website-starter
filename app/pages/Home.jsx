import React from 'react';
import './Home.scss';

export default React.createClass({
  render() {
    return (
      <section id="home">
        <header>Home</header>
      </section>
    );
  }
});
