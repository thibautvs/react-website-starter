#main-menu {
  padding: 20px 0 0 20px;

  a {
    color: #00c3e6;
    text-decoration: none;

    &.active {
      text-decoration: underline;
    }

    &:not(:first-child) {
      margin-left: 8px;
    }
  }
}
